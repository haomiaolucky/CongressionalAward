# Server Configuration
PORT=3000
NODE_ENV=development

# Azure MySQL Database Configuration
DB_HOST=your-azure-mysql-server.mysql.database.azure.com
DB_USER=your_username@your-server
DB_PASSWORD=your_password
DB_NAME=congressional_award_tracker
DB_PORT=3306

# JWT Secret (generate a random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT Expiration (in days)
JWT_EXPIRES_IN=7d

# Email Configuration (using Gmail SMTP as example)
# For production, consider using SendGrid or Azure Communication Services
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_FROM=Congressional Award Tracker <noreply@congressionalaward.org>

# Application URL (for email links)
APP_URL=http://localhost:3000

# Admin Email (for notifications)
ADMIN_EMAIL=admin@congressionalaward.org

# Verification Token Expiry (in hours)
TOKEN_EXPIRY_HOURS=168

# Azure Blob Storage Configuration (Optional - for production use)
# If not configured, files will be stored locally in /uploads directory
# See AZURE_BLOB_STORAGE_SETUP.md for detailed setup instructions
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=youraccountname;AccountKey=yourkey;EndpointSuffix=core.windows.net
AZURE_STORAGE_CONTAINER_NAME=proof-images
